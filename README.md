# 🍜 点餐系统 (Restaurant Ordering System)

面向中小型餐厅的一体化点餐管理系统，支持桌位管理、菜品点餐、上菜队列、结账收银、营业统计与历史查询。

提供 PC 端与移动端自适应界面，可通过 Electron 打包为 Windows 桌面应用。

## 功能特性

- 🪑 桌位管理 — 开台、换桌、合并桌位
- 📋 菜品点餐 — 分类浏览、加购、备注、退菜
- 🍳 上菜队列 — 后厨出菜进度实时追踪
- 💰 结账收银 — 多种支付方式、打折、免单
- 📊 营业统计 — 日/周/月营收、菜品销量排行 (ECharts)
- 📱 移动端适配 — 手机/平板自动切换移动布局
- 🔐 PIN 登录鉴权 — 4位PIN码快速登录，安全问题找回

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite 6 + Pinia + Naive UI + TailwindCSS 3 + ECharts |
| 后端 | Express 4 + TypeScript + SQLite (better-sqlite3) |
| 桌面端 | Electron 33 |
| 实时通信 | SSE (Server-Sent Events) |

## 快速开始

### 环境要求

- Node.js >= 18 (推荐 20+)
- npm

### 安装与运行

```bash
# 安装依赖
npm install
cd server && npm install
cd ../web && npm install
cd ..

# 开发模式 (后端 :3000 + 前端 :5173)
npm run dev

# 打包 Windows 桌面应用
npm run dist
```

## 项目结构

```
diancan/
├── server/          # Express REST API 后端
│   └── src/
│       ├── modules/ # 业务模块 (menu/tables/orders/stats/auth...)
│       ├── db/      # SQLite 数据库 + 迁移文件
│       └── shared/  # 公共类型、中间件
├── web/             # Vue 3 前端 SPA
│   └── src/
│       ├── views/   # 页面 (PC + Mobile 双路由)
│       ├── stores/  # Pinia 状态管理
│       └── api/     # API 请求封装
└── electron/        # Electron 桌面壳
```

## License

MIT
