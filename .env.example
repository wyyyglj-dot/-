# Application port
PORT=3000

# SQLite database path (must be under the mounted /app/data volume)
DB_PATH=/app/data/diancan.db

# Allowed CORS origin(s) — set to specific domain in production
CORS_ORIGIN=*

# Admin token for privileged system endpoints (strongly recommended)
# ADMIN_TOKEN=change-me-to-a-strong-random-value
